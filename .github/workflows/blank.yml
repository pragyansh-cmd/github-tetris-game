name: Build Tetris Game

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Compile
      run: g++ tetris.cpp -o tetris

    - name: Run
      run: ./tetris
